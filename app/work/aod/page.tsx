import type { Metadata } from "next";
import { PlaceholderCaseStudy } from "@/components/placeholder-case-study";

export const metadata: Metadata = {
  title: "Analytics on Demand for Optum — Helen Cai",
  description: "project case study in progress, check back soon!",
};

export default function AodPage() {
  return <PlaceholderCaseStudy />;
}
